<!DOCTYPE html>
<html>
<head>
    <title>Editable Map</title>
    <style>
        svg path {
            cursor: pointer;
            transition: fill 0.2s;
        }
    </style>
</head>
<body>
    <h1>Edit the Map</h1>
    <svg width="600" height="400" viewBox="0 0 1000 600" xmlns="http://www.w3.org/2000/svg">
        <!-- Very simple example — you can swap in a more detailed SVG later -->
        <path d="M100,100 L300,100 L300,300 L100,300 Z" fill="#ccc" stroke="#000" data-name="Country A"/>
        <path d="M350,100 L550,100 L550,300 L350,300 Z" fill="#ccc" stroke="#000" data-name="Country B"/>
    </svg>

    <script>
        document.querySelectorAll("svg path").forEach(region => {
            region.addEventListener("click", () => {
                const selected = region.dataset.selected === "true";
                region.style.fill = selected ? "#ccc" : "#00ccff";
                region.dataset.selected = !selected;
            });
        });
    </script>
</body>
</html>
